<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Daksh Garg — Mechatronics Engineer</title>
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet" />
<style>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --bg: #0d1117;
    --surface: #161b22;
    --surface-2: #1c2128;
    --border: #30363d;
    --border-light: #21262d;
    --text: #e6edf3;
    --text-secondary: #c9d1d9;
    --muted: #7d8590;
    --accent: #58a6ff;
    --accent-hover: #79b8ff;
    --accent2: #3fb950;
    --tag-bg: #1a2332;
    --tag-text: #79c0ff;
    --font-sans: 'Inter', sans-serif;
    --font-mono: 'IBM Plex Mono', monospace;
    --radius: 10px;
    --radius-sm: 6px;
  }

  html { scroll-behavior: smooth; }

  body {
    background: var(--bg);
    color: var(--text);
    font-family: var(--font-sans);
    font-size: 15px;
    line-height: 1.6;
    min-height: 100vh;
    -webkit-font-smoothing: antialiased;
  }

  /* ── NAV ── */
  nav {
    position: sticky;
    top: 0;
    z-index: 100;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 2.5rem;
    height: 58px;
    background: rgba(13,17,23,0.9);
    backdrop-filter: blur(16px);
    border-bottom: 1px solid var(--border-light);
  }

  .nav-logo {
    font-family: var(--font-mono);
    font-size: 13px;
    color: var(--accent);
    letter-spacing: 0.05em;
    display: flex;
    align-items: center;
    gap: 6px;
  }

  .nav-logo .slash { color: var(--muted); }

  .nav-links {
    display: flex;
    gap: 0.25rem;
    list-style: none;
  }

  .nav-links a {
    color: var(--muted);
    text-decoration: none;
    font-size: 13px;
    font-weight: 500;
    padding: 5px 12px;
    border-radius: var(--radius-sm);
    transition: color 0.18s, background 0.18s;
  }

  .nav-links a:hover {
    color: var(--text);
    background: var(--surface);
  }

  /* ── HERO ── */
  .hero {
    max-width: 900px;
    margin: 0 auto;
    padding: 5.5rem 2.5rem 3rem;
    display: flex;
    gap: 3rem;
    align-items: flex-start;
  }

  .avatar-wrap {
    flex-shrink: 0;
    position: relative;
  }

  .avatar {
    width: 96px;
    height: 96px;
    border-radius: 50%;
    background: linear-gradient(135deg, #1c4ed8 0%, #58a6ff 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-mono);
    font-size: 30px;
    font-weight: 500;
    color: #fff;
    border: 2px solid var(--border);
    box-shadow: 0 0 0 4px rgba(88,166,255,0.1);
  }

  .status-badge {
    position: absolute;
    bottom: 2px;
    right: 2px;
    width: 18px;
    height: 18px;
    background: var(--bg);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .status-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: var(--accent2);
    animation: pulse 2.5s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.5; transform: scale(0.85); }
  }

  .hero-text h1 {
    font-size: 2.1rem;
    font-weight: 700;
    letter-spacing: -0.03em;
    color: var(--text);
    margin-bottom: 0.3rem;
    line-height: 1.2;
  }

  .hero-text .tagline {
    font-family: var(--font-mono);
    font-size: 12.5px;
    color: var(--accent);
    margin-bottom: 1rem;
    letter-spacing: 0.02em;
  }

  .hero-text .bio {
    color: var(--text-secondary);
    font-size: 14.5px;
    max-width: 540px;
    line-height: 1.75;
    margin-bottom: 1.25rem;
    font-weight: 400;
  }

  .hero-chips {
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem;
    font-size: 12px;
    margin-bottom: 1.5rem;
  }

  .hero-chip {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    color: var(--muted);
    background: var(--surface);
    border: 1px solid var(--border-light);
    border-radius: 20px;
    padding: 4px 12px;
    font-size: 12px;
    font-weight: 500;
  }

  .cta-row {
    display: flex;
    gap: 0.65rem;
    flex-wrap: wrap;
  }

  .btn {
    font-family: var(--font-sans);
    font-size: 13px;
    font-weight: 500;
    padding: 8px 18px;
    border-radius: var(--radius-sm);
    border: 1px solid var(--border);
    text-decoration: none;
    cursor: pointer;
    transition: all 0.16s;
    display: inline-flex;
    align-items: center;
    gap: 7px;
    letter-spacing: -0.01em;
  }

  .btn-primary {
    background: #1f6feb;
    color: #fff;
    border-color: #1f6feb;
    box-shadow: 0 1px 3px rgba(31,111,235,0.3);
  }

  .btn-primary:hover {
    background: #388bfd;
    border-color: #388bfd;
    box-shadow: 0 2px 8px rgba(56,139,253,0.35);
  }

  /* ── LAYOUT ── */
  .container {
    max-width: 900px;
    margin: 0 auto;
    padding: 0 2.5rem 5rem;
    display: grid;
    grid-template-columns: 1fr 272px;
    gap: 2rem;
    align-items: start;
  }

  /* ── SECTION ── */
  section { margin-bottom: 2.25rem; }

  .section-header {
    display: flex;
    align-items: center;
    gap: 0.65rem;
    margin-bottom: 1.1rem;
    padding-bottom: 0.6rem;
    border-bottom: 1px solid var(--border-light);
  }

  .section-header h2 {
    font-size: 13px;
    font-weight: 600;
    color: var(--text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }

  .section-header .icon { font-size: 15px; }

  /* ── EXPERIENCE CARD ── */
  .exp-card {
    background: var(--surface);
    border: 1px solid var(--border-light);
    border-radius: var(--radius);
    padding: 1.2rem 1.4rem;
    margin-bottom: 0.85rem;
    transition: border-color 0.2s, box-shadow 0.2s;
    position: relative;
    overflow: hidden;
  }

  .exp-card::before {
    content: '';
    position: absolute;
    top: 0; left: 0;
    width: 3px;
    height: 100%;
    background: linear-gradient(180deg, #1f6feb, #58a6ff);
    border-radius: 3px 0 0 3px;
    opacity: 0;
    transition: opacity 0.2s;
  }

  .exp-card:hover {
    border-color: rgba(88,166,255,0.3);
    box-shadow: 0 4px 16px rgba(0,0,0,0.25);
  }

  .exp-card:hover::before { opacity: 1; }

  .exp-header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 0.4rem;
    flex-wrap: wrap;
  }

  .exp-title {
    font-size: 14px;
    font-weight: 600;
    color: var(--accent);
    line-height: 1.3;
  }

  .exp-date {
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--muted);
    white-space: nowrap;
    background: var(--surface-2);
    padding: 3px 8px;
    border-radius: 4px;
    border: 1px solid var(--border-light);
    flex-shrink: 0;
  }

  .exp-subtitle {
    font-size: 12.5px;
    color: var(--muted);
    margin-bottom: 0.75rem;
    font-style: italic;
  }

  .exp-card ul {
    padding-left: 0;
    margin: 0;
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 5px;
  }

  .exp-card ul li {
    font-size: 13px;
    color: var(--text-secondary);
    line-height: 1.55;
    display: flex;
    align-items: baseline;
    gap: 8px;
  }

  .exp-card ul li::before {
    content: '▸';
    color: var(--accent);
    font-size: 10px;
    flex-shrink: 0;
    margin-top: 1px;
  }

  .tech-row {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-top: 1rem;
    padding-top: 0.85rem;
    border-top: 1px solid var(--border-light);
  }

  .tech-pill {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--muted);
    background: var(--surface-2);
    padding: 3px 10px;
    border-radius: 20px;
    border: 1px solid var(--border-light);
  }

  .lang-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    flex-shrink: 0;
  }

  /* ── SKILLS ── */
  .skills-grid {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
  }

  .tag {
    display: inline-flex;
    align-items: center;
    background: var(--tag-bg);
    color: var(--tag-text);
    font-family: var(--font-mono);
    font-size: 11.5px;
    padding: 5px 12px;
    border-radius: 20px;
    border: 1px solid rgba(88,166,255,0.15);
    font-weight: 400;
    transition: border-color 0.15s, background 0.15s;
    cursor: default;
  }

  .tag:hover { border-color: rgba(88,166,255,0.4); background: #1c2d4a; }
  .tag.green { color: #7ee787; background: #0d2a17; border-color: rgba(63,185,80,0.15); }
  .tag.green:hover { border-color: rgba(63,185,80,0.4); background: #142b1c; }
  .tag.orange { color: #ffa657; background: #2d1a00; border-color: rgba(255,166,87,0.15); }
  .tag.orange:hover { border-color: rgba(255,166,87,0.4); }

  /* ── SIDEBAR CARD ── */
  .side-card {
    background: var(--surface);
    border: 1px solid var(--border-light);
    border-radius: var(--radius);
    padding: 1.1rem 1.3rem;
    margin-bottom: 1rem;
  }

  .side-card h3 {
    font-size: 11px;
    font-weight: 600;
    color: var(--muted);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 1rem;
  }

  .edu-entry {
    padding-bottom: 0.9rem;
    margin-bottom: 0.9rem;
    border-bottom: 1px solid var(--border-light);
  }

  .edu-entry:last-child { border-bottom: none; padding-bottom: 0; margin-bottom: 0; }

  .edu-entry .school {
    font-size: 13.5px;
    color: var(--text);
    font-weight: 600;
    line-height: 1.3;
    margin-bottom: 2px;
  }

  .edu-entry .degree {
    font-size: 12px;
    color: var(--muted);
    font-family: var(--font-mono);
    margin-bottom: 2px;
  }

  .edu-entry .years {
    font-size: 11.5px;
    color: var(--muted);
    font-style: italic;
  }

  /* ── ACHIEVEMENTS ── */
  .achievement {
    display: flex;
    align-items: flex-start;
    gap: 10px;
    padding: 9px 0;
    border-bottom: 1px solid var(--border-light);
  }

  .achievement:last-child { border-bottom: none; }

  .badge {
    flex-shrink: 0;
    width: 30px;
    height: 30px;
    background: var(--surface-2);
    border: 1px solid var(--border-light);
    border-radius: 7px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 14px;
  }

  .achievement p {
    font-size: 12.5px;
    color: var(--text-secondary);
    line-height: 1.5;
    padding-top: 4px;
  }

  /* ── CONTACT ── */
  .contact-row { display: flex; gap: 0.5rem; flex-wrap: wrap; }

  .contact-chip {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 12px;
    font-weight: 500;
    color: var(--muted);
    text-decoration: none;
    padding: 6px 14px;
    border: 1px solid var(--border-light);
    border-radius: 20px;
    background: var(--surface-2);
    transition: border-color 0.18s, color 0.18s, background 0.18s;
  }

  .contact-chip:hover {
    color: var(--accent);
    border-color: rgba(88,166,255,0.4);
    background: var(--tag-bg);
  }

  /* ── ACTIVITY ── */
  .contrib-wrap { overflow: hidden; }

  .contrib-grid {
    display: flex;
    gap: 3px;
    flex-wrap: nowrap;
  }

  .contrib-col { display: flex; flex-direction: column; gap: 3px; }

  .contrib-cell {
    width: 11px;
    height: 11px;
    border-radius: 2px;
    background: var(--border-light);
  }

  .contrib-cell.l1 { background: #0e4429; }
  .contrib-cell.l2 { background: #006d32; }
  .contrib-cell.l3 { background: #26a641; }
  .contrib-cell.l4 { background: #39d353; }

  /* ── FOOTER ── */
  footer {
    text-align: center;
    padding: 2rem;
    font-size: 12px;
    color: var(--muted);
    border-top: 1px solid var(--border-light);
  }

  /* ── ANIMATIONS ── */
  .fade-up {
    opacity: 0;
    transform: translateY(14px);
    animation: fadeUp 0.55s cubic-bezier(.22,.68,0,1.2) forwards;
  }

  @keyframes fadeUp {
    to { opacity: 1; transform: translateY(0); }
  }

  .fade-up:nth-child(1) { animation-delay: 0.04s; }
  .fade-up:nth-child(2) { animation-delay: 0.1s; }
  .fade-up:nth-child(3) { animation-delay: 0.16s; }
  .fade-up:nth-child(4) { animation-delay: 0.22s; }

  /* ── RESPONSIVE ── */
  @media (max-width: 700px) {
    .container { grid-template-columns: 1fr; }
    .hero { flex-direction: column; gap: 1.5rem; padding: 3.5rem 1.5rem 2rem; }
    nav { padding: 0 1.25rem; }
    .nav-links { display: none; }
    .container { padding: 0 1.5rem 4rem; }
  }
</style>
</head>
<body>

<nav>
  <span class="nav-logo">daksh-garg <span class="slash">/</span></span>
  <ul class="nav-links">
    <li><a href="#experience">Experience</a></li>
    <li><a href="#skills">Skills</a></li>
    <li><a href="#education">Education</a></li>
    <li><a href="#contact">Contact</a></li>
  </ul>
</nav>

<div class="hero">
  <div class="avatar-wrap">
    <div class="avatar">DG</div>
    <div class="status-badge"><div class="status-dot"></div></div>
  </div>
  <div class="hero-text fade-up">
    <h1>Daksh Garg</h1>
    <p class="tagline">// Mechatronics Engineer · Robotics · Research</p>
    <p class="bio">Mechatronics Engineering student at Kennesaw State University. Building at the intersection of robotics, embedded systems, and applied research — from AGI Core research to SAE Formula race cars.</p>
    <div class="hero-chips">
      <span class="hero-chip">📍 Georgia, USA</span>
      <span class="hero-chip">🎓 KSU Class of 2028</span>
      <span class="hero-chip">✅ Open to Opportunities</span>
    </div>
    <div class="cta-row">
      <a href="mailto:garg.daksh0906@gmail.com" class="btn btn-primary">✉ Get in Touch</a>
    </div>
  </div>
</div>

<div class="container">
  <main>

    <section id="experience">
      <div class="section-header">
        <span class="icon">⚙️</span>
        <h2>Experience</h2>
      </div>

      <div class="exp-card fade-up">
        <div class="exp-header">
          <div class="exp-title">AGI-Core Research Group — First Year Scholars Research</div>
          <span class="exp-date">2025 – Present</span>
        </div>
        <div class="exp-subtitle">Robotics &amp; Autonomous Systems Research</div>
        <ul>
          <li>Designing and implementing robust Python and C++ solutions for robotics pipelines</li>
          <li>Performing multi-sensor calibration and implementing basic sensor fusion techniques</li>
          <li>Presenting research outcomes using clear visuals and concise summaries</li>
        </ul>
        <div class="tech-row">
          <span class="tech-pill"><span class="lang-dot" style="background:#3572A5"></span>Python</span>
          <span class="tech-pill"><span class="lang-dot" style="background:#f34b7d"></span>C++</span>
          <span class="tech-pill"><span class="lang-dot" style="background:#89e051"></span>ROS 2</span>
        </div>
      </div>

      <div class="exp-card fade-up">
        <div class="exp-header">
          <div class="exp-title">SAE Formula Team — Powertrain &amp; DAQ</div>
          <span class="exp-date">2025 – Present</span>
        </div>
        <div class="exp-subtitle">Embedded Instrumentation &amp; Real-Time Telemetry</div>
        <ul>
          <li>Implemented strain-gauge instrumentation on critical suspension structures for accurate stress assessment</li>
          <li>Monitored car vitals during practice races and communicated information efficiently with the driver</li>
        </ul>
        <div class="tech-row">
          <span class="tech-pill"><span class="lang-dot" style="background:#e34c26"></span>Embedded C</span>
          <span class="tech-pill"><span class="lang-dot" style="background:#f1e05a"></span>DAQ Systems</span>
        </div>
      </div>

      <div class="exp-card fade-up">
        <div class="exp-header">
          <div class="exp-title">FRC Robotics — Robot &amp; Arena Captain</div>
          <span class="exp-date">2022 – 2025</span>
        </div>
        <div class="exp-subtitle">Competition Robotics — Design, Build &amp; Operations</div>
        <ul>
          <li>Led and supervised a team of more than 10 individuals across three competitive seasons</li>
          <li>Efficiently repaired robot damage within limited timeframes during live competitions</li>
          <li>Designed and constructed a fully functional field for an off-season intra-school competition</li>
        </ul>
        <div class="tech-row">
          <span class="tech-pill"><span class="lang-dot" style="background:#3572A5"></span>Python</span>
          <span class="tech-pill"><span class="lang-dot" style="background:#178600"></span>Java</span>
          <span class="tech-pill"><span class="lang-dot" style="background:#f34b7d"></span>Mechanical Design</span>
        </div>
      </div>
    </section>

    <section id="skills">
      <div class="section-header">
        <span class="icon">🛠️</span>
        <h2>Skills &amp; Tools</h2>
      </div>
      <div class="skills-grid">
        <span class="tag">Python</span>
        <span class="tag">HTML</span>
        <span class="tag">C++</span>
        <span class="tag green">ROS 2</span>
        <span class="tag green">SolidWorks</span>
        <span class="tag green">Autodesk CAD</span>
        <span class="tag orange">MS Office</span>
        <span class="tag orange">Excel</span>
        <span class="tag orange">PowerPoint</span>
        <span class="tag">Sensor Fusion</span>
        <span class="tag">Strain Gauges</span>
        <span class="tag">DAQ Systems</span>
        <span class="tag green">Hindi (Fluent)</span>
      </div>
    </section>

    <section>
      <div class="section-header">
        <span class="icon">📊</span>
        <h2>Activity</h2>
      </div>
      <div class="side-card">
        <div class="contrib-wrap">
          <div class="contrib-grid" id="contrib"></div>
        </div>
        <p style="font-family:var(--font-mono);font-size:11px;color:var(--muted);margin-top:0.7rem;">Contributions this year</p>
      </div>
    </section>

  </main>

  <aside>

    <div class="side-card" id="education">
      <h3>Education</h3>
      <div class="edu-entry">
        <div class="school">Kennesaw State University</div>
        <div class="degree">B.S. Mechatronics Engineering</div>
        <div class="years">2025 – Expected 2028</div>
      </div>
      <div class="edu-entry">
        <div class="school">Innovation Academy</div>
        <div class="degree">High School Diploma</div>
        <div class="years">2021 – 2025</div>
      </div>
    </div>

    <div class="side-card">
      <h3>Achievements</h3>
      <div class="achievement">
        <div class="badge">🏢</div>
        <p>Founded <strong>Closing the Divide</strong> — Georgia Branch</p>
      </div>
      <div class="achievement">
        <div class="badge">🥋</div>
        <p>Taekwondo Black Belt, 2nd Degree</p>
      </div>
      <div class="achievement">
        <div class="badge">🎾</div>
        <p>Chattahoochee High School Varsity Tennis</p>
      </div>
      <div class="achievement">
        <div class="badge">🏅</div>
        <p>Gold PVSA Volunteering Award (100+ hours)</p>
      </div>
    </div>

    <div class="side-card" id="contact">
      <h3>Contact</h3>
      <div class="contact-row">
        <a href="mailto:garg.daksh0906@gmail.com" class="contact-chip">✉ Email Me</a>
      </div>
    </div>

  </aside>
</div>

<footer>
  <p>Daksh Garg &nbsp;·&nbsp; Mechatronics Engineer &nbsp;·&nbsp; Georgia, USA &nbsp;·&nbsp; 2025</p>
</footer>

<script>
  const grid = document.getElementById('contrib');
  const levels = [0, 0, 0, 1, 2, 3, 4];
  const pick = () => levels[Math.floor(Math.random() * levels.length)];
  const classFor = l => l === 0 ? '' : `l${l}`;
  for (let w = 0; w < 35; w++) {
    const col = document.createElement('div');
    col.className = 'contrib-col';
    for (let d = 0; d < 7; d++) {
      const cell = document.createElement('div');
      cell.className = 'contrib-cell ' + classFor(pick());
      col.appendChild(cell);
    }
    grid.appendChild(col);
  }
</script>
</body>
</html>
